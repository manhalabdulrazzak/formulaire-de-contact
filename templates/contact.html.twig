{% extends 'base.html.twig' %}
{% block body %}
    <main role="main">
        <div class="container">
            <div class="card ">
                <span class="contact2-form-title">
						 {{ contact.object }}
                </span>
                <p> {{ contact.message }}</p>

            </div>
        </div>
    </main>

{% endblock body %}